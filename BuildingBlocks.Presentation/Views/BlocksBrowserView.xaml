<UserControl x:Class="BuildingBlocks.Presentation.Views.BlocksBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BuildingBlocks.Presentation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label>dupa dupa chuj</Label>
        <ListView Name="BlocksListView" ItemsSource="{Binding Blocks}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Width: " />
                        <TextBlock Text="{Binding Width}" FontWeight="Bold" />
                        <TextBlock Text=", Height: " />
                        <TextBlock Text="{Binding Heigth}" FontWeight="Bold" />

                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
